# Legacy / template kopije (veliki folderi — ne za Git niti Vercel)
public_html (stari sajt)/
Sajt kupljen novi template zip/

# dependencies
node_modules
npm-debug.log
yarn-error.log

# build output
.next
.next-stale-*/
out
dist
tsconfig.tsbuildinfo

# Full MySQL exports (veliki fajl, produkcijski podaci — ne commitovati)
agyc3416_santos_sa2025.sql

# Lokalni admin nalozi (hash lozinke — nikad na remote)
data/admin-users.json

# Slučajni / privremeni fajlovi u rootu
.tmp_old_preview.tsx
_SuitPreview.diff
Sajt kupljen novi template zip.zip

# Local environment files and secrets. Keep only redacted templates.
.env*
!.env.example
!**/.env.example

# OS junk
.DS_Store
Thumbs.db

# backend (jer je već na cPanelu)

# cPanel-only PHP/backend runtime
*.php
/cpanel-cron/
/cpanel-upload/
**/error_log

# backend assets (local only)
custom-suits-backend/
transparent-upload/

# local artifacts (previews/tests)
/app/uploads/
/btn/
/latest*.png
/reference.png
/image.png
/pants.png
/our-*.png
/out-mask*.png
/temp-*.png
/tmp-*.png
/white-mask.png
/stripe reference.png

# agent / local temp logs at repo root
.tmp-*

# local research/reference folders
/hockerty-httpcopier/
/docs/
/golden/
/.tmp/
/vest/

# local screenshots / comparison captures
/preview*.png
/pants*.png
/pantspreview*.png

# Local Claude state (worktrees, launch config)
.claude/

# local dev logs + agent config
next-*.log
.mcp.json

.vercel
